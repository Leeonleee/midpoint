{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon index.js",
    "build-frontend": "npm run build --prefix ../frontend",
    "move-assets": "mkdir dist && copyfiles -f ../frontend/dist/**/* ./dist/assets",
    "move-dist": "copyfiles -f ../frontend/dist/** ./dist",
    "build": "npm run build-frontend && npm run move-assets && npm run move-dist"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@google/generative-ai": "^0.17.1",
    "axios": "^1.7.5",
    "copyfiles": "^2.4.1",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "cors": "^2.8.5",
    "nodemon": "^3.1.4"
  }
}
